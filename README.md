# WakeOnLanArduino
This is an arduino version to wake a PC that is on the same private network than an Arduino Board. Using Firebase RealTime Database as the intermediate "server", so that i dont need to port forward neither set a fixed IP.
